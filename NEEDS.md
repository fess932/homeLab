# Что нужно от владельца

---

## 2026-09-24 14:23 — Проверка Docker-образа на amd64 и arm64

Здесь нет Docker, поэтому образ не собирался и не запускался. Нужно на машине с Docker: `docker compose up -d` (образ `ghcr.io/fess932/homelab:latest` публикует CI), затем `docker compose exec homedeck homedeck setup-token` и вход в UI. Что проверить: read-only FS + tmpfs /tmp, работа под uid 1000, лимит 1 GiB, остановку за 60 с (`docker compose stop`). Образ для обеих архитектур собирает и публикует CI (`.github/workflows/ci.yml`, job image).

---

## 2026-09-24 14:23 — Измерения из раздела 11 ТЗ

Нужен стенд: Linux amd64, 2 vCPU, 2 GiB RAM, SSD, плюс 24 часа прогона на малом и расширенном профиле. Генератор нагрузки не писал: нужен набор реальных или синтетических node_exporter/cAdvisor. Скажи, где есть стенд или сколько экспортеров поднять синтетически, — подготовлю скрипт нагрузки и отчёт (p95, peak RAM, прирост диска).

---

## 2026-09-24 14:23 — Коммиты

Ничего не коммитил: правило «коммит только по просьбе». Всё лежит в рабочем дереве. Твои незакоммиченные правки README.md сохранены по смыслу и дополнены ссылками. Скажи, если нужно разбить на коммиты по этапам и/или открыть PR.

---

## 2026-09-24 14:23 — Рекомендуемые настройки при установке

- `HOMEDECK_ALLOWED_HOSTS` — задать имена/IP, по которым открывается панель: это закрывает DNS rebinding для setup и публичной страницы. По умолчанию проверка выключена, чтобы первый запуск работал по любому адресу.
- `HOMEDECK_TRUSTED_PROXIES` — если панель за reverse proxy с HTTPS, иначе cookie будут без Secure.
